.sidebar
  .sidebar__top
    %h5.sidebar__username= current_user.name
    .sidebar__icon
      =link_to new_group_path do
        =fa_icon "pencil-square-o"
      =link_to edit_user_registration_path do
        = fa_icon "cog"

  = render partial: 'shared/group', collection: current_user.groups.includes(:messages)
