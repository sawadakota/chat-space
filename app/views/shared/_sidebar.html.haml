.sidebar
  .sidebar__top
    %h5.sidebar__username= current_user.name
    .sidebar__icon
      =link_to new_group_path do
        =fa_icon "pencil-square-o"
      =link_to edit_user_registration_path do
        = fa_icon "cog"

  = render partial: 'groups/group', collection: current_user.groups.includes(:messages)




  / - @groups.each do |group|
  /   = link_to group_path(group) do
  /     .sidebar__group
  /       %p.sidebar__groupname= group.name
  /       %p.sidebar__latest-massage= group.messages
